import React from 'react';
import Header from '../components/Header';

const Infos = () => {
    
    return (
        <div>
             <Header name="Infos" />
        </div>
    );
};

export default Infos;